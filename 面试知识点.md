# 结构化文档解析模块 — 面试知识点

> 本项目实现了一个面向个人知识库（RAG）的文档解析与分块模块，支持 PDF（PyMuPDF）和 Markdown 格式。

---

## 一、RAG 整体架构（⭐ 必问）

### 1.1 RAG 是什么？

RAG（Retrieval-Augmented Generation，检索增强生成）是在 LLM 生成答案之前，先从外部知识库中检索相关文档片段，拼接到 Prompt 中再让 LLM 生成。相比微调，RAG 成本低、可实时更新知识库、幻觉更少。

### 1.2 RAG 的核心流程

```
用户提问 → Query → Embedding → 向量检索 → 召回Top-K Chunk → 拼入Prompt → LLM生成
```

本模块负责的是 **Ingestion（摄入）** 环节：原始文件 → 解析 → 分块 → 嵌入 → 存入向量数据库。

### 1.3 面试追问：RAG 有哪些常见问题？

| 问题 | 说明 |
|------|------|
| **chunk 切分不当** | 太大则检索不准，太小则丢失上下文 |
| **检索不相关** | 召回的不是用户想要的，需要调整 embedding 或 chunk 策略 |
| **Lost in the Middle** | LLM 对长上下文中间位置的信息关注度下降 |
| **知识库更新** | 增量更新 vs 全量重建的权衡 |

---

## 二、分块策略（⭐⭐ 核心考点）

### 2.1 为什么要分块？

1. **Embedding 模型的 context window 有限**（如 text-embedding-3-small 是 8192 token，但实际最佳效果在 512 token 以内）
2. **检索精度**：小块更精准，大块上下文更完整
3. **成本**：chunk 越小，检索后拼入 prompt 的 token 越少

### 2.2 本项目采用的分块策略

**递归字符分割 + 结构感知**，这是 LangChain、LlamaIndex 等框架的标准做法。

```
分隔符优先级（从粗到细）：
  \n\n  (段落边界)
  → \n  (行边界)
  → .   (句子边界)
  → 。  (中文句号)
  → 空格 (单词边界)
  → 字符级硬切（兜底）
```

**核心参数**：
- `chunk_size = 1024` 字符（≈ 中文 400-500 token，英文 250 token）
- `chunk_overlap = 200` 字符（约 20% 重叠）

### 2.3 面试追问：chunk_size 和 chunk_overlap 怎么选？

**chunk_size**：
- 太小（<256 chars）：单个 chunk 语义不完整，检索碎片化
- 太大（>2048 chars）：噪声多，检索精度下降，且超出 embedding 最优区间
- **经验值**：512-1024 chars 是 SOTA 实践中的 sweet spot

**chunk_overlap**：
- 目的：防止关键信息刚好落在 chunk 边界上被切断
- 太小（<5%）：失去 overlap 的意义
- 太大（>30%）：冗余过多，检索时召回大量重复内容
- **经验值**：10-20% of chunk_size

**关键权衡**：
```
chunk_size ↑ → 上下文更完整，但检索精度 ↓
chunk_size ↓ → 检索更精准，但可能丢失上下文
chunk_overlap ↑ → 信息更连贯，但存储和检索冗余 ↑
```

### 2.4 面试追问：除了递归分割，还有哪些分块方法？

| 方法 | 原理 | 适用场景 |
|------|------|----------|
| **固定大小分割** | 按字符数硬切 | 简单但效果差，不推荐 |
| **递归字符分割**（本项目） | 按分隔符优先级递归切 | **最通用，也是本项目采用的方式** |
| **语义分割** | 用 embedding 相似度判断断点 | 效果好但计算成本高 |
| **句子分割** | 按句号/换行切 | 简单文档 |
| **结构感知分割**（本项目） | 按标题/章节硬边界切 | 结构化文档、论文 |

### 2.5 本项目的"结构感知"具体怎么实现？

**PDF**：通过 PyMuPDF 提取每个文本块的字体大小，取**最小常见字号**（不是中位数！）作为正文字号基线，大于基线 1.3x 的判定为标题，作为 section 硬分割边界。

**Markdown**：解析 `#` 标题层级，每个 `#` 标题作为新的 section 边界。

**效果**：每个 section 内部再递归分割，确保不会跨 section 合并——比如"方法论"和"实验结果"不会出现在同一个 chunk 里。

---

## 三、PDF 解析：PyMuPDF (fitz)（⭐⭐ 技术亮点）

### 3.1 为什么用 PyMuPDF 而不是 pdfplumber / PyPDF2？

| 库 | 优势 | 劣势 |
|----|------|------|
| **PyMuPDF** (本项目) | 速度最快（C 实现），支持块级文本+字体+位置信息 | API 稍复杂 |
| pdfplumber | 表格提取好，API 友好 | 速度慢 |
| PyPDF2 | 纯 Python | 功能弱、速度慢 |

### 3.2 `get_text("dict")` vs `get_text("text")`

```python
# 简单文本（丢失结构信息）
text = page.get_text("text")

# 块级字典（保留字体、位置、块类型） ← 本项目使用
blocks = page.get_text("dict")["blocks"]
```

`get_text("dict")` 返回的数据结构：
```
block → lines → spans → {text, font, size, flags, bbox}
```

### 3.3 标题检测算法（⭐ 面试可能追问）

```
核心思路：正文是文档中最常见的、字号最小的文本

算法：
1. 收集全文档所有 span 的 font size
2. 取出现次数 >= 2 的最小字号 → 正文基线 (body_size)
3. 对每个文本块：
   - 平均字号 >= body_size * 1.3 且文本长度 < 300 → 标题
   - 平均字号 >= body_size * 1.15 且（加粗 或 文本 < 80 chars）→ 标题
```

**为什么不用中位数而用最小常见字号？**
中位数会被大量标题字号拉高（如本例中 11/16 各占一半，中位数是 16），导致标题检测失效。正文是最小且出现次数最多的字号——这个假设在学术论文中成立。

**flags 的含义**：PyMuPDF 中 span 的 `flags` 是位掩码：
- bit 0 (1): superscript
- bit 1 (2): italic  
- bit 2 (4): serif
- bit 3 (8): monospace
- bit 4 (16): bold ← 本项目关注这个

### 3.4 面试追问：PDF 解析有哪些难点？

1. **多栏布局**：需按阅读顺序排序，不能简单按 y 坐标
2. **表格提取**：PyMuPDF 有实验性表格支持
3. **扫描件 PDF**：需要 OCR（本项目暂未实现，后续加入）
4. **中文字体**：CJK 字体的 flags 检测可能不准确
5. **页眉页脚**：需过滤重复内容

---

## 四、Markdown 解析

### 4.1 核心思路

```python
正则匹配 ^#{1,6}\s+(.+) 识别标题
代码块 (```) 作为原子单元，内部不拆分
```

### 4.2 为什么代码块要"原子化"？

如果在代码块中间切分，chunk 中会出现不完整的代码片段，embedding 质量差，检索后也无法使用。保持代码块的完整性对技术文档知识库至关重要。

---

## 五、数据模型设计（⭐ 设计考点）

### 5.1 为什么用 dataclass 而不是 dict？

- 类型安全，IDE 有补全
- 可读性强
- 可以添加方法（如 `to_dict()`）
- 面试中能体现"设计意识"

### 5.2 ChunkMetadata 包含哪些字段？为什么？

```python
@dataclass
class ChunkMetadata:
    source_path: str        # 来源文件 → 检索后可溯源
    page_start: int | None  # 起始页 → PDF 中定位原文
    page_end: int | None    # 结束页
    section_title: str      # 所属章节 → 提供层级上下文
    chunk_index: int        # 当前 chunk 序号
    total_chunks: int       # 总 chunk 数 → 了解文档规模
```

面试重点：**为什么需要 section_title？** 因为 embedding 检索时，chunk 脱离原文后丢失了层级上下文，section_title 作为 metadata 可以让 LLM 知道这段内容属于哪个章节，提升回答质量。

---

## 六、代码设计要点

### 6.1 管道模式

```python
# 统一入口，自动检测文件类型
process_file(path) → (Document, list[Chunk])
    ├── parse_file(path) → Document    # 按扩展名分发
    │   ├── .pdf → pdf_parser.parse_pdf()
    │   └── .md  → markdown_parser.parse_markdown()
    └── chunk_document(doc) → list[Chunk]
```

### 6.2 设计原则体现

| 原则 | 体现 |
|------|------|
| **单一职责** | parser 只管解析，chunker 只管分块，pipeline 只做编排 |
| **开闭原则** | 新增格式只需加 parser 和 pipeline 中的扩展名映射 |
| **依赖倒置** | 所有模块依赖抽象 Document/Chunk 而非具体实现 |

---

## 七、面试模拟问答（⭐⭐⭐）

### Q1: 你为什么不用 LangChain 的 TextSplitter？

> LangChain 太重了，只为一个 splitter 引入整个框架不值得。我的实现只用了 ~60 行，核心逻辑完全一致——递归按分隔符优先级切分 + 合并小块 + overlap。轻量化也意味着更容易调试和定制，比如我加入了结构感知分割（按 section 标题硬切），这在 LangChain 中需要额外配置。

### Q2: chunk 的大小你是怎么确定的？

> chunk_size=1024，chunk_overlap=200。这个选择基于两个考量：一是 embedding 模型（如 text-embedding-3-small）在 512 token 以内效果最好，1024 字符对中文来说大约 400-500 token，对英文约 250 token，都在最优范围内；二是 overlap 20% 是业界实践验证过的平衡点——太低会丢失边界信息，太高会引入过多冗余。当然这可以参数化调整，不同场景可以不同配置。

### Q3: 如果遇到一个没有标题的 PDF，你的系统怎么处理？

> 退化为纯递归字符分割——标题检测失败时 sections 为空，chunker 会走 else 分支直接用 split_text() 处理全文。分隔符优先级（段落→行→句子→单词）仍然能保证语义单元的完整性。

### Q4: 多栏 PDF 你怎么处理？

> 当前版本假设单栏布局，按 y 坐标排序拼接。PyMuPDF 的 get_text("dict") 提供了每个 block 的 bbox 坐标，多栏排版可以通过检测 x 坐标的聚类来识别栏位，然后按"栏内先自上而下，栏间自左而右"的顺序重新排序。这是一个已知的改进方向。

### Q5: 这个系统如何处理图片？

> 当前版本跳过图片。后续会加入两个路径：对于有文字信息的图片（图表、截图），用 OCR 提取文字；对于需要理解视觉内容的（流程图、架构图），用 VLM 生成文字描述。两种结果都会作为额外的"文本"拼入所在页面的 chunk 中。

---

## 八、技术栈速览

| 组件 | 选择 | 原因 |
|------|------|------|
| PDF 解析 | PyMuPDF (fitz) | C 实现，速度快，提供字体/位置元数据 |
| Markdown 解析 | mistune AST | 结构化 token 树，精确识别标题/代码块/图片 |
| 图片处理 | OCR (Tesseract/EasyOCR) + VLM (DeepSeek) | 双路径提取图片文字与视觉语义 |
| 分块算法 | 自研递归分割 | 与 LangChain 等价但零依赖，+结构感知 |
| 关键词检索 | 自研 BM25 (jieba 分词) | 精确关键词匹配，与向量互补 |
| 混合融合 | RRF (Reciprocal Rank Fusion) | 排名级融合，无需分数归一化 |
| 精排 | Cross-Encoder (sentence-transformers) | query-doc 联合编码，精度远超 Bi-Encoder |
| Embedding | sentence-transformers / DeepSeek API | 本地免费 / API 质量更高 |
| 向量数据库 | ChromaDB | 个人 RAG 首选，持久化 + 元数据过滤 + upsert |
| LLM | DeepSeek (OpenAI 兼容协议) | 128K 上下文，中文强，性价比高 |
| RAG 编排 | LangChain LCEL | Pipeline 可读性强，stream/batch/invoke 统一接口 |
| 数据模型 | Python dataclass | 简洁、类型安全 |

---

## 九、Embedding 模型（⭐⭐ 核心考点）

### 9.1 Embedding 是什么？

Embedding 是将文本映射到高维向量空间的过程。语义相近的文本，其向量在空间中距离也相近。这个性质是语义搜索的基础。

```
"猫在沙发上睡觉" → [0.12, -0.34, 0.78, ..., 0.05]  (384维)
"一只猫躺在沙发上" → [0.11, -0.33, 0.79, ..., 0.04]  ← 方向几乎相同
"今天天气很好"     → [-0.78, 0.45, -0.12, ..., 0.67] ← 方向完全不同
```

### 9.2 本项目支持的 Embedding 方案

```
BaseEmbedder (抽象基类)
├── LocalEmbedder      → sentence-transformers 本地模型
├── OpenAIEmbedder     → OpenAI 兼容 API
└── DummyEmbedder      → 离线测试用（hash 确定性向量）
```

**策略模式**：向量存储和检索只依赖 `BaseEmbedder` 接口，Embedder 可以随时替换。

### 9.3 面试追问：为什么默认选 all-MiniLM-L6-v2？

| 指标 | all-MiniLM-L6-v2 | text-embedding-3-small |
|------|------------------|------------------------|
| 维度 | 384 | 1536 |
| 速度 | 极快（本地 CPU） | API 延迟 |
| 成本 | 免费 | $0.02/1M token |
| MTEB 排名 | 中等 | 高 |
| 适用 | 原型、个人项目 | 生产环境 |

**关键取舍**：384 维在大多数场景下效果足够，存储空间小（每条 chunk 384 个 float = ~1.5KB），本地运行零成本且无 API 限流。

### 9.4 面试追问：为什么 embed 之后要做 normalize？

```python
embeddings = self._model.encode(texts, normalize_embeddings=True)
```

Normalize 后将向量的 L2 范数变为 1，此时：
- **余弦相似度 = 向量点积**，计算更快
- ChromaDB 的 `hnsw:space: cosine` 索引依赖向量归一化

公式：`cos_sim(A, B) = A·B / (|A| × |B|)`，当 |A|=|B|=1 时，`cos_sim = A·B`

### 9.5 Embedding 维度对存储和检索的影响

| 维度 | 每条 chunk 存储 | 100万 chunk | 检索速度 |
|------|----------------|-------------|----------|
| 384 | ~1.5 KB | ~1.4 GB | 快 |
| 768 | ~3 KB | ~2.9 GB | 中等 |
| 1536 | ~6 KB | ~5.7 GB | 慢 |

**高维诅咒**：维度越高，最近邻和次近邻的距离差异越小，检索区分度反而下降。

---

## 十、向量数据库：ChromaDB（⭐⭐ 核心考点）

### 10.1 为什么需要向量数据库？

关系型数据库不能高效做"语义搜索"——你不能用 SQL 表达"这段内容和我的问题意思相近"。向量数据库专门为 ANN（Approximate Nearest Neighbor）搜索优化，能在毫秒级从百万向量中召回 Top-K。

### 10.2 为什么选 ChromaDB？

| 方案 | 适用场景 | 劣势 |
|------|----------|------|
| **ChromaDB** (本项目) | 个人 RAG、原型 | 分布式能力弱 |
| FAISS | 亿级向量、极致性能 | 无元数据存储，需自己搭 |
| Milvus | 企业级、分布式 | 部署重 |
| Pinecone | 全托管、零运维 | 收费、网络依赖 |

ChromaDB 的核心优势：**内置 metadata 存储 + 过滤**，不需要像 FAISS 那样自己维护 ID 映射表。

### 10.3 ChromaDB 内部原理（面试深度）

```
存储层：SQLite3（持久化元数据 + 文档）
索引层：HNSW（Hierarchical Navigable Small World）
```

**HNSW 索引原理**（⭐ 高频考点）：

HNSW 是一种图索引结构，分多层：
- **底层**：包含所有节点，边的长度短（局部搜索）
- **上层**：包含部分节点（"高速公路"），边的长度长（全局跳跃）
- **搜索过程**：从顶层开始，贪心向右移动，到达局部最优后下降到下一层，重复直到底层

**复杂度**：搜索 O(log N)，构建 O(N log N)

**面试一句话总结**：HNSW 通过在多层图中建立"高速公路"（长边）和"街区小路"（短边），实现了对数级别的搜索复杂度。

### 10.4 ChromaDB 的元数据过滤

```python
# 只在特定文件中搜索
results = store.search(
    "machine learning",
    top_k=5,
    where={"source_path": "/path/to/paper.pdf"}
)

# 按页码范围过滤
results = store.search(
    "methodology",
    top_k=5,
    where={"page_start": 2}
)
```

这是 **Hybrid Search（混合检索）** 的简单形式：向量相似度 + 元数据条件。在生产环境中会进一步结合 BM25（关键词匹配）和向量搜索。

### 10.5 面试追问：向量数据库 vs 传统数据库 + pgvector？

> pgvector 是 PostgreSQL 的向量扩展，适合已经在用 PG 的团队。但如果从零开始构建个人知识库，ChromaDB 更轻量——零配置、Python 原生、内置 HNSW 索引。两者的核心算法（ANN）是一样的，区别在于运维复杂度。

---

## 十一、检索策略（⭐⭐ 高级考点）

### 11.1 基础语义搜索

```python
retriever.retrieve("什么是 transformer?", top_k=5)
```

流程：query → embedding → 向量空间中找最近邻 → 返回 top_k 个 chunk。

### 11.2 MMR（Maximum Marginal Relevance）

```python
retriever.retrieve_with_mmr(
    "什么是 transformer?",
    top_k=5,
    lambda_mult=0.5  # 相关性权重
)
```

**MMR 算法原理**（⭐ 面试必问）：

```
MMR = λ × sim(Q, Di) - (1-λ) × max(sim(Di, Dj))
      相关项              冗余惩罚项
```

- λ=1：纯相关性排序（退化为基础搜索）
- λ=0：纯多样性（返回最不相似的文档）
- λ=0.5：平衡（默认值）

**为什么需要 MMR？** 在知识库中，可能有多个 chunk 来自同一段落，基础搜索会把它们一起返回（得分都高），造成上下文冗余。MMR 在选第 2 个结果时会惩罚与第 1 个结果相似的候选，强制多样性。

**面试场景**：面试官问"如何避免检索出重复内容？" → 回答 MMR。

### 11.3 元数据过滤

```python
# 只搜索指定论文的内容
retriever.retrieve("实验结果", top_k=5,
    where={"source_path": "/papers/deep-learning-survey.pdf"})
```

实际应用：用户上传了 100 篇论文，搜索时可以限定在特定论文或特定章节中，提升召回精度。

### 11.4 面试追问：如何评估检索质量？

| 指标 | 含义 | 计算 |
|------|------|------|
| **Recall@K** | Top-K 中命中了多少相关文档 | 命中数 / 总相关数 |
| **MRR** (Mean Reciprocal Rank) | 第一个相关结果排在第几位 | 1/rank 的均值 |
| **NDCG** | 考虑排序位置的相关性 | 折损累积增益 |

---

## 十二、新增面试模拟问答（⭐⭐⭐）

### Q6: 你的 embedding 模型怎么选？本地还是 API？

> 我的设计支持两种方式，通过 BaseEmbedder 抽象可以随时切换。本地用 sentence-transformers 的 all-MiniLM-L6-v2，384 维，完全免费，适合原型和个人知识库。如果需要更好效果，可以切到 OpenAIEmbedder 用 text-embedding-3-small（1536 维）。关键是策略模式让切换零成本——VectorStore 和 Retriever 只依赖接口，不关心具体 embedder。

### Q7: 向量数据库为什么选 ChromaDB 而不是 FAISS？

> ChromaDB 自带 metadata 存储和过滤，我的 chunk 包含 source_path、section_title、page 等信息，存入 ChromaDB 后可以直接按条件过滤搜索。FAISS 只存向量，需要自己维护 ID→metadata 的映射表。对于个人知识库这种规模（几千到几万条），ChromaDB 的 HNSW 索引完全够用，而开发体验好很多。

### Q8: ChromaDB 的持久化是怎么实现的？

> ChromaDB 底层用 SQLite3 存储元数据和文档，向量索引（HNSW）以二进制格式持久化到磁盘。PersistentClient 指定一个目录，所有数据都在里面。重启后重新打开同一个路径，数据完好无损。所以我的 VectorStore 支持跨 session 使用——建一次索引，下次直接用。

### Q9: DummyEmbedder 是做什么的？为什么不直接用真实模型？

> DummyEmbedder 用 SHA-256 哈希生成确定性向量——相同文本永远得到相同向量。它不能理解语义，但能让 pipeline 在没有网络的离线环境中跑通测试。这体现了"接口隔离"的好处：因为 VectorStore 只依赖 BaseEmbedder 接口，换成 DummyEmbedder 整个链路照样 work。实际上网后把 DummyEmbedder() 换成 LocalEmbedder() 即可获得真正的语义搜索。

### Q10: 你的 RAG 系统端到端流程是怎样的？

> 1. `process_file(path)` → 解析 PDF/Markdown → Chunk 列表（每个 chunk 带 metadata）
> 2. `store.add_chunks(chunks)` → Embedding → 存入 ChromaDB（持久化）
> 3. `retriever.retrieve(query)` → query embedding → HNSW ANN 搜索 → 返回 top_k chunk
> 4. 把检索到的 chunk 拼入 prompt → LLM 生成答案

### Q11: 如果要扩展到百万级文档，你会改进哪些方面？

> 1. **Embedding 加速**：批量 embed + GPU 推理（sentence-transformers 原生支持 batch）
> 2. **分布式向量库**：迁移到 Milvus 或 Qdrant 集群
> 3. **混合检索**：引入 BM25 做关键词召回，与向量召回结果做 RRF (Reciprocal Rank Fusion) 合并
> 4. **重排序 (Rerank)**：用 Cross-Encoder 对粗召回结果做精排
> 5. **增量更新**：按 source_path 粒度增删，避免全量重建
> 6. **缓存热门 query**：对高频查询结果做缓存

---

## 十三、整体架构图（面试时画出来加分）

```
┌──────────┐    ┌─────────────┐    ┌──────────────┐    ┌──────────┐    ┌──────────────┐
│  PDF/MD  │───▶│ parse_file  │───▶│ ImageProcessor│───▶│ Chunk[]  │───▶│ VectorStore  │
│  文件    │    │ (类型分发)   │    │ (OCR+VLM)    │    │ (带metadata)│   │ .add_chunks  │
└──────────┘    └─────────────┘    └──────────────┘    └──────────┘    └──────┬───────┘
                     │                                                         │
                     │                                                         ▼
              ┌──────┴──────┐                                         ┌──────────────┐
              │ pdf_parser  │                                         │  ChromaDB    │
              │ md_parser   │                                         │  (HNSW索引)   │
              └──────┬──────┘                                         └──────┬───────┘
                     │                                                       │
                     │ 图片提取 + OCR/VLM                                     │
                     └───────────────────────────────────────────────────────┘
                                                                              │
                                                     ┌────────────────────────┘
                                                     ▼
                                              ┌──────────────┐     ┌──────────────┐
                                              │  Retriever   │────▶│   LLM 生成   │
                                              │ .retrieve()  │     │  (RAG 答案)  │
                                              └──────────────┘     └──────────────┘
```

---

## 十四、OCR 与 VLM：多模态文档处理（⭐⭐ 高级考点）

### 14.1 图片处理的时机

图片处理发生在**解析阶段（第一步）**，在分块之前。这是关键设计决策：

```
PDF → 提取文字块 + 提取图片
    → OCR提取图片中文字
    → VLM生成图片视觉描述
    → 将描述插入原文（替代图片位置）
    → 统一分块 → Embedding → 检索
```

**为什么不是在检索之后？** 如果图片信息不进入 embedding，就无法被语义搜索命中。比如用户搜索"chart showing model accuracy"，chunk 里必须包含图片描述才能被检索到。

### 14.2 双路径处理架构

```python
class ImageProcessor:
    def process(self, image_bytes, context=""):
        ocr_text = self.ocr.extract(image_bytes)    # 路径1
        vlm_desc = self.vlm.describe(image_bytes)    # 路径2
        return f"[图片内容] OCR文字: {ocr_text} | 视觉描述: {vlm_desc}"
```

**OCR（光学字符识别）**：提取图片中的文字——图表标签、截图文字、扫描件
**VLM（视觉语言模型）**：理解图片内容——架构图、流程图、数据可视化

### 14.3 OCR 引擎对比

| 引擎 | 安装 | 中文支持 | 速度 | 适用场景 |
|------|------|----------|------|----------|
| **Tesseract** | 系统级 `apt install` | chi_sim 语言包 | 快 | 清晰文档、扫描件 |
| **EasyOCR** | `pip install` | 内置 80+ 语言 | 慢但准 | 复杂场景、手写体 |
| PaddleOCR | pip install | 中文最优 | 中等 | 中文为主场景 |

本项目支持 Tesseract 和 EasyOCR，通过 `BaseOCR` 接口切换。

### 14.4 VLM 的作用

VLM 解决的是 OCR 搞不定的问题：
- **架构图**："这是一个三层的 Transformer 架构，包括 Encoder、Attention、Decoder"
- **数据图表**："柱状图显示 Model A 准确率 92%，Model B 78%，横轴是模型名称"
- **照片/截图**：任何没有文字但有视觉信息的内容

### 14.5 面试追问：图片描述如何插入原文？

> 按阅读顺序（y 坐标 → x 坐标排序）处理每个 page 的所有 block。文字块直接拼接，图片块先用 OCR+VLM 生成描述文字，再插入到原位置。这样图片的语义信息就融入了周围文字的上下文中，分块后也能被检索到。

### 14.6 面试追问：VLM 的 Prompt 怎么设计？

```python
prompt = "请详细描述这张图片的内容，包括图表类型、关键数据"
if context:
    prompt = f"图片出现在以下上下文中:\n{context}\n\n{prompt}"
```

> 关键是把图片周围的文字（context）也传给 VLM——这样它知道图片在文章中的位置，能给出更准确的描述。比如知道图片在"实验结果"章节，就会侧重描述实验数据而非随意发挥。

### 14.7 面试追问：OCR/VLM 失败怎么办？

> 图片处理器用 try/except 包裹每个步骤，OCR 失败不影响 VLM，两者都失败就跳过该图片（降级为纯文字文档）。生产环境中会加日志和重试（VLM API 有 rate limit）。

### 14.8 多模态 RAG 的未来方向

面试中可以提到：
1. **ColPali/ColQwen**：直接用视觉 embedding 模型处理 PDF 页面截图，跳过 OCR
2. **Multi-vector retrieval**：一个 chunk 对应多个向量（文字向量 + 图片向量）
3. **Late-interaction**：ColBERT 风格，先粗召回再 token 级匹配

---

## 十五、完整项目架构总结

```
Layer 1: 解析 (parsing/)
    pdf_parser.py       → PyMuPDF 文字提取 + 字体检测标题
    markdown_parser.py  → mistune AST 解析（标题/代码块/图片）
    ↓
Layer 2: 图片处理 (imageproc/)
    ocr.py      → Tesseract / EasyOCR
    vlm.py      → OpenAI Vision / DeepSeekVLM / Dummy
    processor.py → OCR + VLM 编排
    ↓
Layer 3: 分块 (parsing/chunker.py)
    递归字符分割 + 结构感知 + overlap
    ↓
Layer 4: Embedding (vectordb/embeddings.py)
    LocalEmbedder / OpenAIEmbedder / DeepSeekEmbedder / DummyEmbedder
    ↓
Layer 5: 混合检索 (retrieval/ + vectordb/)
    bm25.py      → BM25 关键词检索（jieba 分词）
    hybrid.py    → RRF 融合（向量 + BM25）
    reranker.py  → Cross-Encoder 精排
    store.py     → ChromaDB (HNSW + upsert 增量更新)
    cache.py     → TTL LRU 缓存
    ↓
Layer 6: RAG 引擎 (llm/)
    deepseek.py  → DeepSeekLLM（OpenAI 兼容协议）
    rag_engine.py → LangChain LCEL Pipeline
                    → Query Rewrite（多角度检索）
                    → Multi-query Retrieval
                    → Context Reorganization
                    → Cache Lookup
```

---

## 十六、LLM 接入（⭐⭐ 核心考点）

### 16.1 DeepSeek API 的接入方式

DeepSeek API 完全兼容 OpenAI SDK 协议，只需改 `base_url`：

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-xxx",
    base_url="https://api.deepseek.com",  # 关键：换这个 URL
)

# Chat 调用完全一样
resp = client.chat.completions.create(
    model="deepseek-chat",        # deepseek-chat / deepseek-reasoner
    messages=[{"role": "user", "content": "你好"}],
    temperature=0.7,
    max_tokens=2048,
)
```

**面试关键点**：OpenAI-compatible 协议是行业标准，Ollama、vLLM、DeepSeek、Groq 等都遵循这个协议。设计时基于 OpenAI SDK + base_url 参数化，可以零成本切换任何兼容厂商。

### 16.2 DeepSeek 模型选择

| 模型 | context | 特点 | 适用 |
|------|---------|------|------|
| `deepseek-chat` | 128K | 通用对话，速度快 | RAG 问答 |
| `deepseek-reasoner` | 128K | 推理增强（R1），输出带 `reasoning_content` | 复杂推理 |

### 16.3 抽象基类设计

```python
class BaseLLM(ABC):
    def generate(self, messages, temperature, max_tokens) -> str: ...
    def generate_stream(self, messages, ...) -> Iterator[str]: ...
```

与 Embedding 模块同样的**策略模式**——RAGEngine 只依赖 `BaseLLM` 接口，切换模型零成本。

### 16.4 流式输出

```python
def generate_stream(self, messages, ...):
    stream = client.chat.completions.create(..., stream=True)
    for chunk in stream:
        if chunk.choices[0].delta.content:
            yield chunk.choices[0].delta.content
```

面试追问："为什么需要流式输出？" → 用户体验：用户不用等 10 秒才看到完整答案，token 级别实时显示。技术本质：SSE (Server-Sent Events) 协议。

---

## 十七、RAG 查询引擎（⭐⭐⭐ 核心考点）

### 17.1 RAG 引擎的完整流程

```
用户提问 "这篇论文用了什么方法？"
    │
    ▼
Retriever.retrieve(query, top_k=5)
    → query embedding → HNSW ANN 搜索 → 5 个相关 chunk
    │
    ▼
RAGEngine._build_messages(question, chunks)
    → System: "你是知识库助手...参考资料：[chunk1]...[chunk5]"
    → User: "这篇论文用了什么方法？"
    │
    ▼
LLM.generate(messages)
    → 基于参考资料生成答案
    → "根据论文，研究方法包括..."
    │
    ▼
返回 {"answer": "...", "sources": [chunk1, ...]}
```

### 17.2 Prompt 工程（⭐ 面试必问）

```python
RAG_SYSTEM_PROMPT = """\
你是一个知识库助手，帮助用户从个人知识库中查找信息。

回答规则：
1. 仅根据下方"参考资料"中的内容回答问题
2. 如果参考资料中没有相关信息，请明确告知用户，不要编造
3. 回答时引用具体的来源（文件名、章节标题）
4. 如果参考资料包含矛盾信息，请指出并分析
5. 用中文回答"""
```

**每条规则的设计意图**：

| 规则 | 解决的问题 |
|------|-----------|
| 仅参考给定资料 | 防止 LLM 用训练数据中的知识"补充"答案（幻觉） |
| 没有就说没有 | 强制诚实，避免编造 |
| 引用来源 | 可追溯性，用户能验证答案 |
| 指出矛盾 | 知识库可能有冲突信息，需要透明化 |
| 指定语言 | 确保输出格式一致 |

**Context 格式设计**：每个 chunk 带 `[序号] 来源 | 章节 | 页码` 前缀，让 LLM 能精确引用。

### 17.3 面试追问：如何防止 RAG 幻觉？

1. **Prompt 约束**：明确"参考资料中没有就说没有"
2. **来源引用**：要求 LLM 标注每条信息的来源
3. **温度控制**：RAG 场景用低 temperature（0.3-0.7），减少随机性
4. **后验证**：用 NLI 模型检查答案是否被 context 支持
5. **Retrieval 质量**：垃圾进垃圾出——好的 chunk 策略是第一道防线

### 17.4 MMR 在 RAG 中的应用

```python
# 普通检索：可能返回多个相似 chunk
engine.query("transformer是什么?", top_k=5, use_mmr=False)

# MMR 检索：去重，返回多样化的 chunk
engine.query("transformer是什么?", top_k=5, use_mmr=True)
```

MMR 在 RAG 中的价值：如果 top-5 中有 3 个 chunk 来自同一段落的拆分结果，LLM 看到的上下文就是冗余的。MMR 确保检索结果的多样性 → prompt 中的信息密度更高。

### 17.5 面试追问：top_k 怎么选？

- top_k 太小（1-2）：信息不足，可能漏掉关键内容
- top_k 太大（>10）：噪声多，prompt 过长，LLM 注意力分散（Lost in the Middle）
- **经验值**：3-5 对简单问题，5-8 对复杂问题

---

## 十八、新增面试模拟问答（⭐⭐⭐）

### Q12: 为什么用 DeepSeek 而不是直接调 OpenAI？

> DeepSeek 性价比极高（价格约为 GPT-4 的 1/10），128K 超长上下文，中文能力强。技术上两者协议完全兼容，我的 `DeepSeekLLM` 继承自 OpenAI-compatible 的调用方式，通过策略模式和 `BaseLLM` 抽象，切回 OpenAI 只需要改 `base_url` 和 `api_key`。

### Q13: 你的 RAG Prompt 为什么这样设计？

> 核心原则是"限制 + 引导"。限制 LLM 只用参考资料回答（防幻觉），引导它引用具体来源（可追溯）。每条规则都对应一个 RAG 常见问题——幻觉、丢失来源、语言不一致。实际调优时可以根据场景调整 system prompt，比如对法律文档加重"精确引用"要求，对科普内容放宽。

### Q14: 流式输出在 RAG 中有什么特殊考虑？

> 流式输出的 chunk 边界可能切断中文字符（一个中文字 3 字节），终端显示可能乱码。另外 RAG 场景中 LLM 会先输出引用的来源标记，再输出答案——流式让用户第一时间看到"正在检索..."的反馈，体验更好。技术上 SSE 协议天然支持，OpenAI SDK 的 `stream=True` 一行开启。

### Q15: 如果要支持多轮对话怎么办？

> 当前 `RAGEngine.query()` 是单轮模式。多轮对话需要：
> 1. 维护 `messages` 历史列表
> 2. 每轮新问题时，用问题本身检索（不是用整个对话历史——会导致检索漂移）
> 3. 把历史对话 + 新检索的 context + 新问题一起发给 LLM
> 4. 可选：对历史对话做摘要压缩，节省 context window

### Q16: 整个项目的核心亮点是什么？

> 1. **全链路自研 + LangChain 编排**：6 个 Layer，核心模块自研（解析/分块/BM25/RRF），LangChain LCEL 做 Pipeline 编排
> 2. **混合召回**：BM25 关键词 + 向量语义 + RRF 融合，比纯向量检索召回率提升显著
> 3. **Cross-Encoder 精排**：粗召回后做 Cross-Encoder rerank，减少无关上下文干扰
> 4. **Query Rewrite**：LLM 多角度改写 query，泛化检索覆盖面
> 5. **多模态支持**：OCR + VLM 双路径处理图片
> 6. **工程细节**：TTL 缓存、增量 upsert、流式输出、元数据过滤

---

## 十九、Markdown AST 解析（mistune）

### 19.1 为什么用 AST 而不是正则？

正则 `^#{1,6}\s+(.+)` 无法处理：
- 代码块中的 `#` 被误判为标题
- 嵌套列表的层级语义
- 图片 `![alt](src)` 的识别
- 引用块、HTML 块等边界情况

AST（Abstract Syntax Tree）将 Markdown 解析为结构化 token 树，每个节点有明确的 type（heading/paragraph/block_code/list），递归遍历即可精确提取。

### 19.2 本项目用 mistune v3 的实现

```python
token_list, _state = mistune.Markdown().parse(raw)
for token in token_list:
    if token["type"] == "heading":      # 标题 → section 边界
    if token["type"] == "block_code":   # 代码块 → 原子保留
    if token["type"] == "paragraph":    # 段落 → 追加到当前 section
```

mistune 是纯 Python、零依赖、速度最快的 Markdown 解析器之一。

---

## 二十、BM25 + RRF 混合召回（⭐⭐⭐ 高频考点）

### 20.1 BM25 算法原理

BM25 是概率检索模型 BM25 的经典实现，核心公式：

```
BM25(D, Q) = Σ IDF(qi) × (tf × (k1+1)) / (tf + k1×(1-b+b×|D|/avgdl))

IDF(qi) = log((N - n(qi) + 0.5) / (n(qi) + 0.5) + 1)
```

| 参数 | 含义 | 默认值 |
|------|------|--------|
| k1 | 词频饱和度控制 | 1.5 |
| b | 文档长度归一化 | 0.75 |
| N | 文档总数 | - |
| avgdl | 平均文档长度 | - |

**面试要点**：k1 控制词频的影响——k1 越小，词频增加对得分的影响越快饱和（一个词出现 5 次和 50 次差别不大）。b 控制文档长度的惩罚——b=1 完全归一化，b=0 不归一化。

### 20.2 为什么需要混合检索？

| 检索方式 | 优势 | 劣势 |
|----------|------|------|
| 向量检索 | 语义理解，"猫"能找到"猫咪" | 对专有名词、数字不敏感 |
| BM25 | 精确关键词匹配，反向索引 | 不理解语义，"猫咪"找不到"猫" |

**互补关系**：用户问"2024年Transformer的改进"，向量检索理解语义，BM25 精确匹配 "2024" 和 "Transformer"。

### 20.3 RRF (Reciprocal Rank Fusion) 融合

```
RRF_score(d) = Σ 1/(k + rank_i(d))
```

- k=60 是经典常数（避免分母为 0，同时降低排名靠前的文档权重差异）
- 不需要分数归一化——直接用排名计算
- 两路检索各自取 top_k*2，RRF 后取 top_k

**面试追问：为什么不直接加权求和？** → 向量相似度和 BM25 分数不在同一个量纲上，直接加权需要做 min-max 归一化，而 RRF 直接用排名，天然量纲统一。

---

## 二十一、Cross-Encoder Rerank（⭐⭐ 核心考点）

### 21.1 Bi-Encoder vs Cross-Encoder

```
Bi-Encoder（向量检索）:
    encode(query) → vec_q
    encode(doc)   → vec_d
    score = cos(vec_q, vec_d)
    → 速度快，query/doc 独立编码，可预计算 doc 向量

Cross-Encoder（精排）:
    encode([query, doc]) → score
    → 速度慢，query 和 doc 联合输入 Transformer，全注意力交互
    → 精度远高于 Bi-Encoder
```

**核心差异**：Bi-Encoder 中 query 和 doc 没有直接交互，Cross-Encoder 中它们在同一层做 cross-attention。

### 21.2 召回→精排的二阶段架构

```
粗召回（高性能）               精排（高精度）
    ↓                              ↓
BM25 + 向量检索 → Top 20     → Cross-Encoder → Top 5
  (Bi-Encoder)                  (re-rank)
```

这个架构是**百度、Google、Bing 等搜索引擎的标准模式**。

### 21.3 面试追问：模型选择

> 默认用 `cross-encoder/ms-marco-MiniLM-L-6-v2`（384 维），在 MS MARCO 上训练，速度与精度平衡。生产级场景可用 `BAAI/bge-reranker-v2-m3`（支持中英文）。实际部署时做 batch inference 提升吞吐。

---

## 二十二、Query Rewrite + 上下文重组（⭐⭐ 高级考点）

### 22.1 Query Rewrite 实现

```python
QUERY_REWRITE_PROMPT = """\
将用户的原始问题改写成 2-3 个更适合检索的查询。
规则：消除代词、扩展同义词、保留语义。"""

def _rewrite_query(self, question: str) -> list[str]:
    raw = self._llm.generate(rewrite_prompt)
    return [original] + [rewritten_queries]
```

改写策略：
1. **代词消解**："它的性能如何？" → "Transformer 模型的性能如何？"
2. **术语对齐**："怎么做微调？" → "fine-tuning 方法 | 微调步骤"
3. **多角度扩展**：一个 query → 2-3 个不同角度的 query

### 22.2 Multi-query 检索

用改写后的多个 query 分别检索，结果合并去重。合并后可能有 15-20 个候选，再送 Cross-Encoder 精排到 top_k。

### 22.3 上下文重组（Context Reorganization）

```python
def _reorganize_context(self, chunks) -> str:
    # 按来源 → 章节分组，添加序号引用
    for i, chunk in enumerate(chunks, 1):
        header = f"[{i}] 来源: {source} | 章节: {section} | 页码: {page}"
```

重组策略：每个 chunk 带 `[序号] 来源 | 章节 | 页码` 前缀，LLM 可以精确引用 `[3]` 来标注答案来源。

---

## 二十三、TTL 缓存 + 增量索引

### 23.1 缓存设计

```python
class QueryCache:
    """TTL + LRU 缓存"""
    def get(self, query) -> dict | None:     # hash 命中 → 返回
    def set(self, query, result, ttl=3600):  # 写入 + LRU 淘汰
    def invalidate_by_source(self, path):    # 源文件更新 → 清相关缓存
```

**为什么需要缓存？** 重复查询直接返回缓存，省去 Embedding API + 检索 + LLM 生成的全部开销。TTL 保证缓存新鲜度，按 source_path 粒度失效保证一致性。

### 23.2 增量索引

```python
store.upsert_chunks(chunks)  # ID 存在则 update，不存在则 insert
store.delete_by_source(path) # 删除旧版本文档的所有 chunk
```

增量更新流程：`delete_by_source(old_path) → parse_file(updated_path) → upsert_chunks(new_chunks)`

### 23.3 面试追问：缓存一致性

> 缓存失效采用主动失效策略——当知识库文件更新时（delete_by_source + upsert），主动调用 `invalidate_by_source` 清理相关缓存。没有用被动 TTL 等待过期，因为过期时间内可能返回旧知识的答案。

---

## 二十四、LangChain LCEL 在项目中的角色（⭐⭐ 高频考点）

### Q17: 为什么用 LangChain？用了哪些部分？

> 项目只用 LangChain 的 **LCEL (LangChain Expression Language)** 做 RAG Pipeline 编排，核心链条：
> ```python
> chain = ChatPromptTemplate | ChatAdapter | StrOutputParser
> chain.invoke({"context": ..., "question": ...})
> ```
> 用 LangChain 而不是自己写的好处：Pipeline 可读性强、支持 `stream`/`batch`/`invoke` 统一接口、PromptTemplate 支持变量注入。但我们**没有**用 LangChain 的 TextSplitter、Retriever、VectorStore（都是用自研模块），因为 LangChain 这些组件太黑盒，出了问题难调试。

### Q18: Hybrid Retrieval 中 BM25 和向量检索的分工？

> BM25 负责精确关键词匹配（专有名词、数字、术语），向量负责语义理解（同义词、模糊表达）。RRF 融合时不依赖原始分数，用排名计算——天然量纲统一。alpha 参数控制两路权重，0.5 是平衡值。实际效果：混合检索的召回率比纯向量高 10-20%（视数据集而定）。

### Q19: 为什么检索后还要 Rerank？

> 粗召回（向量+BM25）速度快但精度有限，Bi-Encoder 中 query 和 doc 没有直接交互。Cross-Encoder 把 query-doc 拼接后进 Transformer 做全注意力交互，精度提升明显。但 Cross-Encoder 慢（不能预计算），所以只能对 Top 20-30 候选做精排。这个"粗召回+精排"的二阶段架构是搜索引擎工业标准。

